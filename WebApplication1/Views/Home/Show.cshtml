@model List<WebApplication1.Models.ProductDTO>
@{
    Layout = "~/Views/Shared/_mylayout.cshtml";

    ViewBag.Title = "Show";
}
<h2>Show</h2>
@{
    foreach (var item in Model)
    {
        <div style="border:1px solid red;width:300px;height:100px;">
             <span>Product ID: @item.ProductID</span> <br />
             <span> Name : @item.Name</span> <br />
             <span>Price: @item.Price</span> <br />
             <span>Created On: @item.CreatedOn</span> <br />
             <span>Created By: @item.CreatedBy</span> <br />

        @*@{
            if (Session["isadmin"] != null)
            {
                Boolean flag = (Boolean)Session["isadmin"];
                if (flag == true)
                {
            <a href="/Home/Delete/@item.ProductID">Delete</a>
            <a href="/Home/Edit/@item.ProductID">Edit</a>
                    }
                }
            }*@
       </div>
        }
    }

@*<table>
    <tr>
        <td>ID</td>
        <td>Name</td>
    </tr>
    @{
        for (int a = 0; a < Model.Count; a++)
        {
        <tr>
            <td>@Model[a].ID</td>
            <td>@Model[a].Name</td>
        </tr>
        }
    }
</table>*@